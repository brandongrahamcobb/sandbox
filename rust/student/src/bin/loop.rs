fn main() {
    // This is the start of a chapter.
    // Loops can have labels beginning with a single quote ie 'counting_up: loop {.
    // They can be affected by inner breaks and continues by following the statement with the 'counting_up name.
    for v in (1..4).rev() {
        // Reverses iterations
    }
}
