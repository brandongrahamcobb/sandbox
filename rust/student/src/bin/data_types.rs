fn main() {
    // Tuples are indexed by the variable and a period followed by the index ie. x.1.
    // Arrays are indexed like python.
    // Arrays are type annotated with [i32; 5] or the same value by defining = [value; size].
    // Tuples are annotated by (i32, f64, etc).
    // See range.rs
}
